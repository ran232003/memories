import React, { useState } from "react";

const MyAlert = () => {
  const [alertObject, setAlert] = useState({
    lable: "",
    showAlert: false,
    cssClass: "",
  });
  return <div></div>;
};

export default MyAlert;
